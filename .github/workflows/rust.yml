name: test

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      - name: Lint
        run: cargo clippy
      - name: Build
        run: cargo build --verbose

